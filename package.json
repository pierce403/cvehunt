{
  "name": "cvehunt-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:data": "uv run python scripts/generate_site_data.py",
    "build": "npm run generate:data && vite build",
    "dev": "npm run generate:data && vite --host 127.0.0.1"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "vite": "^8.0.16",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "lucide-react": "^0.562.0"
  },
  "devDependencies": {}
}
